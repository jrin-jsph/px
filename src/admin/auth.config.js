// auth.config.js

// IMPORTANT: Replace this entire logic with Firebase Authentication (signInWithEmailAndPassword) 
// before deploying to production with actual data. 
// Hardcoded credentials are strictly for the mock frontend build.

export const ADMIN_AUTH = {
  EMAIL: "admin@propertyexpress.com",
  PASSWORD: "adminpassword123"
};
